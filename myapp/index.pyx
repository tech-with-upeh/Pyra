/*

navh = "80px"

div_id = 'nav'


def add() {
    return 1
}
app() {
    page("my tetst page",id="kkk", cls="oppppp", style={
        "height":"100vh",
        "background-color":"pink"
    }) {
        view() {
            text("Hello World!", cls="hellocls", style={
                "font-size":"24px",
                "color":"blue"
            })
        }
        text("mydiv",  cls="pllllll", style={
        "height": navh,
        "background-color":"green"
    })
        img("img.png", style={
            "height":"200px",
            "width": "200px"
        })
        }
    }

*/
app() {
    page() {
        view("sdd", oncnjs() {
            
            print("view clicked")
            djkdk()
        }) {
            p= 23 +o
            msmsk()
        }
    }
}